package com.kike.training.inquiry.domain.model;

public record WelcomeMessage(String message) {
    // Un simple registro (record) de Java para el mensaje de bienvenida.
    // Los 'records' son inmutables y son excelentes para objetos de valor.
}
